# 编码与积木维护约定

## 积木维护规则

每次修改涉及业务流程的代码时，必须同步更新对应积木：

1. **找到受影响的积木**：根据修改文件的路径匹配锚点
2. **更新节点逻辑**：处理步骤变了就改
3. **更新 Mermaid 图**：调用链变了就改（新增/删除服务间调用）
4. **追加变更记录**：一行说明改了什么 + MR 号
5. **如果是新流程**：创建新积木文件并注册到 `_index.md`（用 `vibe-blocks new`）

### 不需要更新积木的情况

- 纯重构（逻辑不变，只是代码结构调整）
- bug fix 且不改变流程语义
- 仅修改日志、注释、格式化

## 积木粒度标准

一个积木 = 一条完整的业务流程（从触发到响应）

- ✅ 创建订单、用户注册、领取优惠券、订单退款
- ❌ "参数校验"（太细）、"订单系统"（太粗）

## 代码分层约定

```
Controller（入口） → Service（业务编排） → Repository（数据访问）
     ↓ Feign / gRPC
下游 Controller → Service → Repository
```

## 服务间通信约定

_根据你的项目补充：使用什么协议（HTTP / gRPC / 消息）、统一响应格式、错误转换。_

## 错误处理约定

- 业务异常统一抛出（如 `ServiceException`）
- 跨服务调用失败统一转换为业务异常
- Controller 层不捕获异常，由全局异常处理器统一处理

## 命名约定

- 接口客户端：`XxxClient`
- 请求 DTO：`XxxRequest`
- 响应 DTO：`XxxDTO` / `XxxResponse`
- 服务类：`XxxService`
- 控制器：`XxxController`

## 不要做的事

- 不要在 BFF 层直接操作下游服务的数据库
- 不要跳过统一响应包装直接使用下游返回值
- 不要硬编码环境相关的 URL，使用配置中心或环境变量
